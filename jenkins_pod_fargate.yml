apiVersion: v1
kind: Pod
metadata:
  name: test
  labels:
    workload: jenkins-fargate
spec:
  serviceAccountName: myjenkins
  securityContext:
    runAsUser: 0
    fsGroup: 0   
     
  
  containers:
  - name: maven-fargate
    image: maven:3.6.0-jdk-8-alpine
    resources:
      requests:
        memory: "128Mi"
        cpu: "512m"
      limits:
        memory: "2048Mi"
        cpu: "1024m"
    command:
    - cat
    tty: true
  
  
