apiVersion: v1
kind: Pod
metadata:
  name: test
  labels:
    workload: jenkins-fargate
spec:
  serviceAccountName: myjenkins
  securityContext:
    runAsUser: 0
    fsGroup: 0   
     
  
  containers:
  - name: maven-fargate
    image: maven:3.6.0-jdk-8-alpine
    resources:
      requests:
        memory: "2048Mi"
        cpu: "1024m"
      limits:
        memory: "8192Mi"
        cpu: "4096m"
    command:
    - cat
    tty: true
