apiVersion: v1
kind: Pod
metadata:
  name: test
  labels:
    workload: jenkins-fargate
spec:
  securityContext:
    runAsUser: 0
    fsGroup: 0


  containers:
  - name: maven-fargate
    image: maven:3.6.0-jdk-8-alpine
    resources:
      requests:
        memory: "2Gi"
        cpu: "512m"
      limits:
        memory: "8Gi"
        cpu: "4096m"
    command:
    - cat
    tty: true
    
    ########################################
    
apiVersion: v1
kind: Pod
metadata:
  name: test
  labels:
    workload: jenkins-fargate
spec:
  securityContext:
    runAsUser: 0
    fsGroup: 0


  containers:
  - name: maven-fargate
    image: maven:3.6.0-jdk-8-alpine
    resources:
      requests:
        memory: "4Gi"
        cpu: 1
      limits:
        memory: "8Gi"
        cpu: 4
    command:
    - cat
    tty: true

